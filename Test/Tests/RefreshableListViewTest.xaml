<Page
    x:Class="Test.Tests.RefreshableListViewTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Test.Tests"
    xmlns:comet="using:Comet.Controls"
    xmlns:data="using:Test.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <comet:RefreshableListView x:Name="listView"
                                   ItemsSource="{x:Bind Items}" 
                                   RefreshActivated="listView_RefreshCommand"
                                   PullProgressChanged="listView_PullProgressChanged">
            <comet:RefreshableListView.ItemTemplate>
                <DataTemplate  x:DataType="data:Item">
                    <TextBlock Text="{x:Bind Title}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap"/>
                </DataTemplate>
            </comet:RefreshableListView.ItemTemplate>
            <comet:RefreshableListView.RefreshIndicatorContent>
                <Border HorizontalAlignment="Center" x:Name="refreshindicator" CornerRadius="30" Height="20" Width="20" ></Border>
            </comet:RefreshableListView.RefreshIndicatorContent>
        </comet:RefreshableListView>
    </Grid>
</Page>
